[build]
  base = "frontend"
  publish = "build"
  command = "npm run build"

[functions]
  directory = "netlify/functions"

# SPA fallback so deep links still work
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
